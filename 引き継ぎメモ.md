# Cursor → Claude (Cowork) 引き継ぎメモ

作成日：2026-04-22
担当：Claude（Cowork）

---

## 1. まず結論

**コードは全部GitHubに残っているので、Cursorを解約しても何も消えません。** 今後は同じGitHubリポジトリをClaudeが読み書きして開発を続けます。

今日の調査で、4プロジェクトのうち3ゲームの中身はすべて確認できました。`purple-park`（本サイト）は非公開リポジトリのため、Claudeからの取得にはひと手間必要です（§4で手順）。

---

## 2. 各プロジェクトの現状

### 🎮 Purple Jumper（public）

- URL: https://purplegames-creator.github.io/purple-jumper/
- GitHub: https://github.com/purplegames-creator/purple-jumper
- 構成：**index.html 1ファイル**に全部入り（HTML/CSS/JS全1846行）
- Supabase：CDN経由で読み込み（スコア送信に使っていると思われる）
- 画像素材：GAMEOVER画像22枚 + 透過済.png など
- 最終更新：2026-04-22「高速バグ」
- 気になる点：`desktop.ini`（Windows自動生成のゴミファイル）がコミットされている

### 🏍️ Purple Biker（public）

- URL: https://purplegames-creator.github.io/purple-biker/
- GitHub: https://github.com/purplegames-creator/purple-biker
- 構成：**index.html 1ファイル**に全部入り（1776行）
- Supabase：未使用（スコア保存なし）
- 画像素材：透過済.png、logo.png、jump.mp3
- 最終更新：2026-04-14

### 🤿 Purple Diver（public・一番しっかりしている）

- URL: https://purplegames-creator.github.io/purple-diver/
- GitHub: https://github.com/purplegames-creator/purple-diver
- 構成：**ファイル分割型**（index.html / game.js / main.js / styles.css など）
- Supabase：**完全統合済み**（今日／週間／歴代ランキング実装済み）
- テーブル名：`diver_scores`（カラム：nickname, score, created_at）
- 特徴：同名ユーザーは最高スコアのみ残す処理あり、上位100位表示
- 最終更新：2026-04-14

👉 **このPurple Diverの構成が一番きれいなので、今後の開発のお手本にできます。**

### 🏥 Purple Park（private）— 本命・医療HP事業サイト

- URL: https://purple-park.vercel.app/
- GitHub: https://github.com/purplegames-creator/purple-park 🔒
- ゲーム3本のハブ兼、医療HP事業の事業サイト
- **現時点ではClaudeが中身を読めていません**（次のアクション §4 参照）

---

## 3. セキュリティで1つ確認したいこと

`purple-diver`の `supabase-config.js` に、SupabaseのURLとanon keyがそのまま書かれてコミットされています。

**結論：致命傷ではないが、確認しておきたい。**

SupabaseのANON keyは「ブラウザに公開される前提」のキーなので、バレてもすぐ悪用されるものではありません。ただし、データベース側で「Row Level Security（RLS）」という保護設定が正しく入っていないと、悪意ある人が `diver_scores` テーブルに無限にデータを入れたり消したりできる状態になります。

👉 **次の開発タイミングで、一緒にSupabaseのRLS設定を確認しましょう。**（今日やる必要はないです）

---

## 4. 今すぐHayatoさんにやってほしいこと（2つだけ）

### ✅ ステップA：Purple Parkフォルダを一度キレイにする

今、このフォルダ（`Purple Park`）に空っぽの `purple-jumper` / `purple-biker` / `purple-diver` / `purple-park` という4つのフォルダができています。これはClaudeが最初にやった取り込みが途中で失敗した残骸です。

**操作手順：**
1. Windowsのエクスプローラーで `Purple Park` フォルダを開く
2. 上記4つのフォルダを **右クリック → 削除**
3. 「隠しファイル」が見えないと中身が空に見えるかもしれませんが、そのまま削除でOK

### ✅ ステップB：Purple Parkリポジトリの扱いを決める

3択あります。おすすめは **②** です。

**① Cursorで使っていた元のフォルダをCoworkに指定する（最速）**
- Cursorで開発していた元フォルダがPC上にすでにあるはず（例：`C:\Users\xxxx\Documents\purple-park` など）
- Coworkの上部「フォルダ選択」から、**そのフォルダに切り替える**だけでOK
- 既に.gitの認証も済んでいるはずなので、Claudeがすぐ読み書き＆push可能

**② GitHubのPersonal Access Token（PAT）を作ってClaudeに渡す（Coworkからpurple-parkを操作したい場合）**
- GitHub右上アイコン → Settings → Developer settings → Personal access tokens → 「Fine-grained tokens」で新規作成
- 対象リポジトリを `purple-park` だけに絞る
- 権限：Contents（Read and write）
- 生成された文字列（`github_pat_xxx...`）をClaudeに貼る

**③ 当面purple-parkはCursorの代替として別ツール（VS Code等）でやる**
- 3ゲームだけClaudeで開発し、Purple Parkサイトは別ルートで管理

---

## 5. Claudeの次のアクション

Hayatoさんから指示があれば、以下を順番に進めます：

1. Purple Park（本丸の医療HP用サイト）の中身を把握
2. 各プロジェクトのVercel接続状態を確認（MCP経由で可能）
3. SupabaseのRLS設定チェック（MCP経由で可能）
4. 次の機能追加 or 医療HP制作に着手

---

## 6. よく使うコマンドメモ（今後の参考用）

Windowsのコマンドプロンプト or Git Bashで、自分のPCにリポジトリを落としたいときは：

```
git clone https://github.com/purplegames-creator/リポジトリ名.git
```

たとえば Purple Diver なら：

```
git clone https://github.com/purplegames-creator/purple-diver.git
```

これで「指定フォルダの下にリポジトリと同じ名前のフォルダができて、中にソースが入る」状態になります。
